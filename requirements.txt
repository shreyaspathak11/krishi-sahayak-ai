# Core AI and Logic - Minimal set
groq
langchain
langchain-community
langchain-core
langchain-groq
langchain-chroma
chromadb

# HuggingFace for embeddings (both options for compatibility)
langchain-huggingface
sentence-transformers

# Web Server - Essential only
fastapi
uvicorn[standard]
pydantic

# Data Processing - Essential only
requests
python-dotenv

# Weather API
pyowm

# Utilities
pytz

# For PDF processing
pypdf

# Optional: For Pinecone (comment out if not using)
# langchain-pinecone
# pinecone-client

# Optional: For advanced ML features (comment out if not needed)
# transformers
# torch

# Optional: For telephony (comment out if not using)
# twilio

# Optional: For OpenAI integration (comment out if not using)
# openai>=1.0.0